export type User = {
    id: string;
    name: string;
    mail: string;
    password: string;
    role: string;
};

export type Hostel = {
    id: string;
    description: string;
    name: string;
    location: string;
    picture_list: string;
};